import styled from 'styled-components';

export const Footer = styled.footer`
  display: flex;
  justify-content: center;
  color: var(--black);
  font: var(--heading-3);
  bottom: -7rem;
  position: relative;
`;
